{
  "name": "emerald-e2e-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Dependencias y configuración exclusiva para tests E2E Playwright (no viaja a producción)",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.43.1"
  }
}
